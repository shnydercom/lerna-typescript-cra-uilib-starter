import React from "react";
import { Header } from "my-monorepo-ui-lib";

export const HeaderFromUILib = () => {
  return (
    <Header
      onLogin={() => {}}
      onCreateAccount={() => {}}
      onLogout={() => {}}
    >
    </Header>
  );
};
