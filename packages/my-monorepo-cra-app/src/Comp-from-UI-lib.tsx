import React from "react";
import { Header } from "my-monorepo-ui-lib";

export const CompFromUILib = () => {
  return (
    <Header onLogin={() => {}} onCreateAccount={() => {}} onLogout={() => {}} />
  );
};
